# name: LANCAST Build

# on:
#   push:
#     branches: [main]
#     tags: ["v*.*.*"]
#   pull_request:
#     branches: [main]

# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

# env:
#   CARGO_TERM_COLOR: always
#   RUST_BACKTRACE: 1

# jobs:
#   lint-and-test:
#     name: Lint & Test
#     runs-on: ubuntu-22.04
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "npm"

#       - name: Setup Rust
#         uses: dtolnay/rust-toolchain@stable
#         with:
#           components: clippy, rustfmt

#       - name: Cache Rust dependencies
#         uses: Swatinem/rust-cache@v2
#         with:
#           workspaces: "./src-tauri -> target"

#       - name: Install Node dependencies
#         run: npm ci

#       - name: Lint (ESLint)
#         run: npm run lint

#       - name: Format check (Prettier)
#         run: npm run format:check

#       - name: Rust fmt check
#         working-directory: src-tauri
#         run: cargo fmt --all -- --check

#       - name: Rust Clippy
#         working-directory: src-tauri
#         run: cargo clippy --all-targets --all-features -- -D warnings

#       - name: Rust tests
#         working-directory: src-tauri
#         run: cargo test --all-features

#   build:
#     name: Build (${{ matrix.platform }})
#     needs: lint-and-test
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           - platform: windows-latest
#             args: ""
#             artifact: "*.msi"
#           - platform: macos-latest
#             args: "--target aarch64-apple-darwin"
#             artifact: "*.dmg"
#           - platform: ubuntu-22.04
#             args: ""
#             artifact: "*.AppImage"

#     runs-on: ${{ matrix.platform }}

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "npm"

#       - name: Setup Rust
#         uses: dtolnay/rust-toolchain@stable
#         with:
#           targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

#       - name: Cache Rust dependencies
#         uses: Swatinem/rust-cache@v2
#         with:
#           workspaces: "./src-tauri -> target"

#       - name: Install Linux system dependencies
#         if: matrix.platform == 'ubuntu-22.04'
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y \
#             libwebkit2gtk-4.1-dev \
#             libssl-dev \
#             libgtk-3-dev \
#             libayatana-appindicator3-dev \
#             librsvg2-dev \
#             patchelf

#       - name: Install Node dependencies
#         run: npm ci

#       - name: Build Tauri application
#         uses: tauri-apps/tauri-action@v0
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
#           TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
#         with:
#           tagName: ${{ github.ref_name }}
#           releaseName: "LANCAST ${{ github.ref_name }}"
#           releaseBody: |
#             ## LANCAST ${{ github.ref_name }}

#             End-to-end encrypted LAN communication. Zero cloud. Zero servers. Zero traces.

#             ### Downloads
#             - **Windows** — `.msi` installer
#             - **macOS** — `.dmg` disk image (Apple Silicon)
#             - **Linux** — `.AppImage` portable binary

#             See [ARCHITECTURE.md](ARCHITECTURE.md) for full technical details.
#           releaseDraft: true
#           prerelease: false
#           args: ${{ matrix.args }}

#   release-check:
#     name: Release gate
#     needs: build
#     if: startsWith(github.ref, 'refs/tags/v')
#     runs-on: ubuntu-22.04
#     steps:
#       - name: All builds passed
#         run: echo "All platform builds succeeded — release is ready for publishing."
